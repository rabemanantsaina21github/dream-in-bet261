{% extends 'email_base.html.twig' %}

{% block body %}
<td class="bodyEmail" bgcolor="#fff">
        <table width="100%" border="0" cellpadding="0" cellspacing="0">
            <tr>
                <td>
                    <h1 class="h1">Bonjour !</h1>
                </td>
            </tr>
            <tr>
                <td class="pb-unpoint-cinq">
                    <p>
                        Pour réinitialiser votre mot de passe, veuillez visiter le lien ci-dessous :
                    </p>
                </td>
            </tr>
            <tr>
                <td class="text-center pb-unpoint-cinq">
                    <a href="{{ url('app_reset_password', {token: resetToken.token}) }}" class="btn-validation-compte">
                    	Réinitialiser mon mot de passe {#{{ url('app_reset_password', {token: resetToken.token}) }}#}
                    </a>
                </td>
            </tr>
            <tr>
                <td>
                    <p>
                        Si vous n'êtes pas à l'origine de ce demande, merci d'ignorer cet email.  Ce lien expirera dans une heure {#{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}#}. 
                    </p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Au revoir !</p>
                </td>
            </tr>
        </table>
    </td>
{% endblock %}
